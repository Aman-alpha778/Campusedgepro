<dialog class="portal-modal" id="{{ $dialogId }}"><div class="portal-modal-body"><div class="portal-card-head"><div><h2>{{ $course ? 'Edit course' : 'Add course' }}</h2></div><button class="portal-button-ghost" onclick="this.closest('dialog').close()">Close</button></div><form class="portal-form-grid" method="post" action="{{ $action }}">@csrf @if($method !== 'post') @method($method) @endif
  <label class="portal-field full"><span>Department</span><select name="department_id" required>@foreach($departments as $department)<option value="{{ $department->id }}" @selected(old('department_id', $course?->department_id) == $department->id)>{{ $department->name }}</option>@endforeach</select></label>
  <label class="portal-field"><span>Name</span><input name="name" value="{{ old('name', $course?->name) }}" required></label><label class="portal-field"><span>Code</span><input name="code" value="{{ old('code', $course?->code) }}" required></label>
  <label class="portal-field"><span>Duration</span><input name="duration" value="{{ old('duration', $course?->duration) }}" required></label><label class="portal-field"><span>Total semesters</span><input type="number" name="total_semesters" value="{{ old('total_semesters', $course?->total_semesters ?? 1) }}" min="1" required></label>
  <label class="portal-field"><span>Status</span><select name="status"><option value="active" @selected(old('status', $course?->status ?? 'active') === 'active')>Active</option><option value="inactive" @selected(old('status', $course?->status) === 'inactive')>Inactive</option></select></label>
  <div class="full"><button class="portal-button">Save course</button></div>
</form></div></dialog>
