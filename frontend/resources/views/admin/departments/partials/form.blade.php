<dialog class="portal-modal" id="{{ $dialogId }}"><div class="portal-modal-body"><div class="portal-card-head"><div><h2>{{ $department ? 'Edit department' : 'Create department' }}</h2></div><button class="portal-button-ghost" onclick="this.closest('dialog').close()">Close</button></div><form class="portal-form-grid" method="post" action="{{ $action }}">@csrf @if($method !== 'post') @method($method) @endif
  <label class="portal-field"><span>Campus</span><select name="campus_id" required>@foreach($campuses as $campus)<option value="{{ $campus->id }}" @selected(old('campus_id', $department?->campus_id) == $campus->id)>{{ $campus->name }}</option>@endforeach</select></label>
  <label class="portal-field"><span>HOD</span><select name="hod_id"><option value="">Unassigned</option>@foreach($hodUsers as $user)<option value="{{ $user->id }}" @selected(old('hod_id', $department?->hod_id) == $user->id)>{{ $user->name }}</option>@endforeach</select></label>
  <label class="portal-field"><span>Name</span><input name="name" value="{{ old('name', $department?->name) }}" required></label><label class="portal-field"><span>Code</span><input name="code" value="{{ old('code', $department?->code) }}" required></label>
  <label class="portal-field"><span>Status</span><select name="status"><option value="active" @selected(old('status', $department?->status ?? 'active') === 'active')>Active</option><option value="inactive" @selected(old('status', $department?->status) === 'inactive')>Inactive</option></select></label>
  <div class="full"><button class="portal-button">Save department</button></div>
</form></div></dialog>
