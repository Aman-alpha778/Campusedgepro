<dialog class="portal-modal" id="{{ $dialogId }}"><div class="portal-modal-body"><div class="portal-card-head"><h2>{{ $user ? 'Edit user' : 'Create user' }}</h2><button class="portal-button-ghost" onclick="this.closest('dialog').close()">Close</button></div><form class="portal-form-grid" method="post" action="{{ $action }}">@csrf @if($method !== 'post') @method($method) @endif
  <label class="portal-field"><span>Name</span><input name="name" value="{{ old('name', $user?->name) }}" required></label><label class="portal-field"><span>Email</span><input type="email" name="email" value="{{ old('email', $user?->email) }}" required></label>
  <label class="portal-field"><span>Phone</span><input name="phone" value="{{ old('phone', $user?->phone) }}"></label><label class="portal-field"><span>Password</span><input type="password" name="password" {{ $user ? '' : 'required' }}></label>
  <label class="portal-field"><span>Status</span><select name="status"><option value="active" @selected(old('status', $user?->status ?? 'active') === 'active')>Active</option><option value="inactive" @selected(old('status', $user?->status) === 'inactive')>Inactive</option></select></label>
  <div class="full portal-checkbox-grid">@foreach($roles as $role)<label class="portal-checkbox"><input type="checkbox" name="roles[]" value="{{ $role->id }}" @checked($user?->roles->contains($role))> {{ $role->name }}</label>@endforeach</div><div class="full"><button class="portal-button">Save user</button></div>
</form></div></dialog>
