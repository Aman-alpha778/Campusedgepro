<dialog class="portal-modal" id="{{ $dialogId }}"><div class="portal-modal-body"><div class="portal-card-head"><div><h2>{{ $member ? 'Edit faculty' : 'Add faculty' }}</h2></div><button class="portal-button-ghost" onclick="this.closest('dialog').close()">Close</button></div><form class="portal-form-grid" method="post" action="{{ $action }}">@csrf @if($method !== 'post') @method($method) @endif
  <label class="portal-field"><span>Name</span><input name="name" value="{{ old('name', $member?->user?->name) }}" required></label><label class="portal-field"><span>Email</span><input type="email" name="email" value="{{ old('email', $member?->user?->email) }}" required></label>
  <label class="portal-field"><span>Phone</span><input name="phone" value="{{ old('phone', $member?->user?->phone) }}"></label><label class="portal-field"><span>Password</span><input type="password" name="password" {{ $member ? '' : 'required' }}></label>
  <label class="portal-field"><span>Department</span><select name="department_id" required>@foreach($departments as $department)<option value="{{ $department->id }}" @selected(old('department_id', $member?->department_id) == $department->id)>{{ $department->name }}</option>@endforeach</select></label><label class="portal-field"><span>Employee ID</span><input name="employee_id" value="{{ old('employee_id', $member?->employee_id) }}" required></label>
  <label class="portal-field"><span>Qualification</span><input name="qualification" value="{{ old('qualification', $member?->qualification) }}"></label><label class="portal-field"><span>Experience</span><input type="number" name="experience" value="{{ old('experience', $member?->experience ?? 0) }}" min="0" required></label>
  <label class="portal-field"><span>Joining date</span><input type="date" name="joining_date" value="{{ old('joining_date', $member?->joining_date?->format('Y-m-d')) }}"></label><label class="portal-field"><span>Salary</span><input type="number" step="0.01" name="salary" value="{{ old('salary', $member?->salary ?? 0) }}" required></label>
  <label class="portal-field"><span>Status</span><select name="status"><option value="active" @selected(old('status', $member?->status ?? 'active') === 'active')>Active</option><option value="inactive" @selected(old('status', $member?->status) === 'inactive')>Inactive</option></select></label><div class="full"><button class="portal-button">Save faculty</button></div>
</form></div></dialog>
