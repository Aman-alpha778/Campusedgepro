# Backend

Laravel application code now lives here: controllers, models, mail classes, middleware, config, routes, database files, storage, and tests.

Run backend commands from the project root as before:

```bash
php artisan test
php artisan serve
composer dump-autoload
```
